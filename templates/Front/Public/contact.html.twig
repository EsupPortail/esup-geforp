{% extends "Front/base.html.twig" %}

{% block header %}
    <header>
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-xs-12">
                    <h3>Plate-forme de gestion des formations</h3>
                    <h1>Contact</h1>
                </div>
            </div><!-- /row -->
        </div> <!-- /container -->
    </header>
{% endblock %}

{% block body %}
<div class="container">
    <br><br>
    <h3>Pour toute question sur le Plan de Formation ou les modalités d'inscription, vous pouvez contacter :
    <br><br>
        {%  for institution in etablissements %}
            <div>{{ institution.name }}</div>
            <div style="text-align: center"><a href="mailto:{{ institution.email }}">{{ institution.email }}</a></div>
            {% if institution.website is defined %}
                <div style="text-align: center"><a href="{{ institution.website }}">{{ institution.website }}</a></div>
            {% endif %}
        {% endfor %}
    <br><br>
    </h3>
</div>
{% endblock body %}
